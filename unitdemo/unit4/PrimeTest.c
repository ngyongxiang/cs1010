#include <stdio.h>
#include <math.h>

int main(void){
		

 return 0;
}

// is prime return 1, if not return 0
int is_prime( int num){
	int i;
	for (i = 2; i <= sqrt(num); i++){
		if (num % i == 0)
			return :0;
	}
	return 1;
}
